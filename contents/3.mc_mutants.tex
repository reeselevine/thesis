% !TeX root = ../main.tex
\chapter[Evaluating and Improving Testing for Memory Consistency Specifications]{Evaluating and Improving Testing for Memory Consistency Specifications}
\label{chap:mc-mutants}

\epigraph{\textit{Mutation. It is the key to our evolution.}}{--- Professor X \\\footnotesize\textit{X-Men}}


\newcommand{\sys}{MC Mutants\xspace}

\section{Personal Context}

I started my PhD at UC Santa Cruz in the fall of 2020 with a basic background in parallel programming, but no experience working with memory models or GPUs. During my first year, I started working on a project to develop new tools for running litmus tests on GPUs, first in Vulkan and later in WebGPU. The WebGPU project was started in collaboration with David Neto and Alan Baker from Google, who were working on the WebGPU specification and the Dawn implementation for Chromium, and who were interested in sponsoring more cross-platform testing of GPU memory models. During this time, I also started working with an undergrad, Tianhao (Tim) Guo, who took the parallel programming class Tyler taught and I TA'd for. Over the summer, Tim did a great job building the initial website we used to run the WebGPU litmus tests, and which later won Distinguished Artifact awards at ASPLOS and ISSTA.

In the fall of my second year, Andi Quinn joined UC Santa Cruz as a new professor and started attending our lab's meetings. Over time, Andi became more involved in developing the story and writing the text of the MC Mutants paper, for which I am grateful. Also in the fall of my second year, I met Raph Levien from Google, who was working on WebGPU initiatives and had run into an issue while trying to develop a prefix-scan implementation. His analysis and reproduction of this issue became the basis of the parallel litmus testing strategy that I extended to work with many classic litmus tests.

During the winter of my second year, we realized that a mutation testing strategy allowed for a more rigorous analysis of how well MCS testing was working, and we submitted MC Mutants to the spring deadline for ASPLOS 2023. After mixed reviews, we received a decision of ``major revision'', which meant that the reviewers were positive on the work, but wanted to see more results before accepting the paper. After some more work (luckily not too much) over the summer, which also coincided with me getting married and starting my first internship at Apple, we received word that MC Mutants had been accepted. MC Mutants was my first-ever published paper, and formed the basis for my PhD advancement and much of the rest of my dissertation work.

\section{Introduction}
\label{sec:mc-mutants-introduction}

This chapter introduces \textit{Memory Consistency Mutants} or \sys, a methodology that quantitatively evaluates MCS testing environments using specification mutation testing~\cite{budd1985specification,demillo1978hints}. Litmus tests are \textit{mutated} so that the erroneous behaviors checked by the tests become allowed. A test environment \textit{kills} a mutated test (mutant) by observing the newly allowed execution. The number of mutants killed is called the \textit{mutation score}. Additionally, \sys records the speed at which mutants are killed, called the \textit{mutant death rate}.

Intuitively, mutants check for an allowed weak memory or fine-grained interleaving behavior that is closely related to a disallowed behavior of an MCS litmus test. The main contribution of \sys is a formal definition of this notion of \textit{related}; \sys generates mutants systematically by disrupting edges in a happens-before cycle of a disallowed behavior, corresponding to a small change in the program syntax of the litmus test.

The effectiveness of a testing environment can be evaluated by its mutation score (and mutant death rate) when executing mutants. Thus, a test environment must aim to maximize legal behavior in the mutants that closely relates to the illegal behavior in the original litmus tests. Hence, there are two constraints upon which \sys depends.

First, the legal mutant behavior must be \textit{observable} on the testing platform. That is, even if the mutant behavior is allowed by the specification, it may not be observable on the testing platform. If the mutant behavior is not observable, then \sys will be unable to evaluate the testing environment with respect to the given mutant and corresponding litmus test. The results in \mysec\ref{sec:mc-mutants-mut-scores} show that most mutant behavior is observable on GPUs across a range of mainstream vendors. However, this might not hold for all platforms or mutants, in which case the mutants should be pruned according to the expected observable behavior on the implementation (discussed further in \mysec\ref{sec:mc-mutants-observed-mutants}).

Second, the validity of \sys depends on a correlation between observing legal mutant behavior and real MCS bugs. The evaluation validates this correlation in three cases by showing that a testing environment's ability to kill a mutant is highly correlated with its ability to find related MCS bugs (\mysec\ref{sec:mc-mutants-corr-analysis}). This also means that testing environments that do not reliably kill mutants may not able to expose the MCS bugs.

\sys enables principled evaluation and optimization of MCS test environments. An optimized testing environment can increase confidence that MCS bugs will be found, if they exist, using the original, unmutated test suite. This work uses \sys to develop two novel testing environment improvements: a parallel testing environment and a testing confidence strategy.

\paragraph{Parallel Testing Environment (PTE)}
While previous work ran litmus tests in parallel on multicore CPUs~\cite{alglave2011litmus}, the effectiveness was limited by the low number of cores found on traditional SMP CPU machines.
As the number of parallel tests increases, it becomes difficult to scale thread-to-test assignment in a way that is safe, efficient, and effective; especially in the presence of tunable stress parameters. Given this, prior techniques for running parallel litmus tests do not generalize to GPUs.

The technique described here overcomes these challenges using a parallel permutation strategy based on modular arithmetic of coprime numbers to assign test instances to threads with low overhead.
This approach, called \textit{Parallel Testing Environments}, or PTE, increases mutant death rates by an average of three orders of magnitude over previous work. Moreover, \sys shows that parallel testing synergizes with the stress testing environment given in prior work~\cite{kirkham2020foundations}, increasing the mutant death rate by an additional 43\%.

Parallel testing environments also enable the discovery of violations of MCSs that previous testing strategies missed. For example, on AMD GPUs, violations of the message passing litmus test with fences shown in \myfig\ref{fig:intro-mprelacq} were seen using WebGPU, readily appearing at a rate of 10.4 violations per second. This resulted in a fix to an AMD Vulkan compiler and a specification change to the WebGPU MCS~\cite{webgpu-spec-change}, as the WebGPU MCS incorrectly asserted that barriers were enough to disallow this behavior across workgroups.

\paragraph{MCS Test Confidence}

It is impossible to have complete confidence that testing will uncover bugs, as litmus tests are non-deterministic. However, \sys can provide a \textit{reproducibility score} for a testing environment. This score represents the statistical confidence that a testing run will kill a mutant given the mutant death rate on previous runs. It is parameterized by a time budget, i.e.\ how long each test can be run for.
Across four GPUs spanning four mainstream vendors, the tuned parallel testing environment provides a mutation score of 82\% and a reproducibility score of 99.999\% with a budget of 64 seconds per test,
matching the maximum mutation score achieved by prior work with $(1/4096)^{\text{th}}$ the time budget. Reproducibility scores provide a principled way to evaluate the relative effectiveness of test environments at finding bugs.


\paragraph{Evaluation Specification} The approach is evaluated in WebGPU. As discussed in \mychap\ref{chap:background}, WebGPU has a hierarchical execution model similar to other GPU platforms, e.g.\ CUDA and OpenCL. Threads are partitioned into workgroups, and there are several different memory regions.

However, the WebGPU MCS differs from other specifications in that it targets devices from a wide range of vendors, e.g., Intel, AMD, Apple, and NVIDIA. In fact, it has the \textit{largest} set of backends targeted by any GPGPU MCS. To provide a portable abstraction across these devices, WebGPU currently has relatively few options for synchronization; there are no sequentially consistent atomic operations as seen in NVIDIA's PTX~\cite{lustig2019ptx}, or OpenCL~\cite{batty2016overhauling}. However, the few ordering properties that WebGPU provides, such as coherency across atomic accesses, must be preserved across all devices. Given this unique context, MCS testing is both \textit{critical}, as it must ensure a single consistent MCS on a diverse set of backends, and \textit{challenging}, as testing environments must be effective across many devices.

This work targets only one level (or \textit{scope}) of the GPU execution hierarchy, specifically threads that are in different workgroups. This scope was selected for three reasons: (1)~it is pragmatic, as there is no other way for inter-workgroup threads to synchronize without the expensive routine of stopping and relaunching the kernel, (2)~prior work developed stress techniques targeting this scope~\cite{kirkham2020foundations}, providing testing environments suitable for tuning and evaluation, and (3)~at the time this project was developed, WebGPU did not yet expose the subgroup (or warp in CUDA) scope, and thus it could not be tested. However, \sys applies generally to MCS testing and can be extended to the more complete GPU execution hierarchy as the specification, stress testing techniques, and application use cases continue to evolve.

Using \sys, a set of 20 litmus tests and 32 mutant tests were created. These tests are evaluated across four GPU devices, spanning four vendors: Apple, AMD, Intel, and NVIDIA, for a total of 128 mutant/device combinations. One major impact of this work is its adoption into the WebGPU conformance test suite (CTS) for testing the MCS (see \mysec\ref{sec:mc-mutants-impact}).

\subsection{Contributions}

In summary, the contributions of this work are:
\begin{enumerate}
    \item \sys: A mutation testing approach that can evaluate MCS testing environments (\mysec\ref{sec:mc-mutants}).
    \item A novel parallel test environment that executes litmus test instances in parallel (\mysec\ref{sec:mc-mutants-parallel-tests}).
    \item An MCS test confidence strategy using a reproducibility score parameterized over a time budget (\mysec\ref{sec:mc-mutants-rep-heuristic}).
    \item A \sys implementation and evaluation in WebGPU. The evaluation spans 4 different GPUs from 4 different vendors and shows that: (\mysec\ref{sec:mc-mutants-evaluation})
    \begin{itemize}
        \item \sys generates a test suite consisting of 20 unmutated litmus tests and 32 mutated litmus tests.
        \item Parallel testing environments are able to kill 81\% more mutants with an average mutant death rate over $2000\times$ higher than prior work.
        \item MCS test confidence can explore trade-offs in reproducibility and time for test engineering.
    \end{itemize}
\end{enumerate}

Two impacts of this work are particularly notable. First, it identified two MCS bugs in WebGPU implementations: a violation of SC-per-location read coherence on an Intel GPU running through Apple's Metal API, and a violation on AMD GPUs of the fenced message-passing example from \myfig\ref{fig:intro-mprelacq} that led to an AMD Vulkan driver fix and a WebGPU specification change. Second, the work has been adopted into the WebGPU CTS (\mysec\ref{sec:mc-mutants-impact}). The effectiveness of parallel test environments was a critical factor in getting the tests added to the CTS, as the full CTS must be run often during development. The MCS test confidence strategy supported a proposed time budget of approximately one minute on average desktop hardware for the full MCS test suite, adding little overhead to the full CTS.

\noindent
The artifact for this work is publicly available~\cite{levine2022mcmutantsartifact}.

\begin{figure}
\centering
\subfloat[3-event reversing \textit{po-loc} mutator]{{\includegraphics[width=0.75\columnwidth]{images/mutator1.pdf}\label{fig:mc-mutants-mutator-1}}} \\
\subfloat[4-event weakening \textit{po-loc} mutator]{{\includegraphics[width=0.75\columnwidth]{images/mutator2.pdf}\label{fig:mc-mutants-mutator-2}}} \\
\subfloat[4-event weakening \textit{sw} mutator]{{\includegraphics[width=0.75\columnwidth]{images/mutator3.pdf}\label{fig:mc-mutants-mutator-3}}}
\caption[The three cycles used to instantiate conformance tests and mutants.]{The three cycles used to instantiate conformance tests and mutants, with conformance test templates shown on the left and mutant templates on the right; \textit{m[x]} signifies an abstract memory event which may be instantiated as a read, write, or RMW. The disrupted events and edges are highlighted in red. \label{fig:mc-mutants-mutators}}
\end{figure}

\begin{table}
\centering
\caption{The mutators defined in \myfig\ref{fig:mc-mutants-mutators} and the number of conformance tests and mutants generated by each.}
\begin{tabular}{l r r}
 \toprule
 \textbf{Mutator} & \textbf{Conformance Tests} & \textbf{Mutants} \\
 \midrule
 Reversing \textit{po-loc} & 8 & 8 \\
 Weakening \textit{po-loc} & 6 & 6 \\
 Weakening $sw$ & 6 & 18 \\
 \midrule
 Combined & 20 & 32 \\
 \bottomrule
\end{tabular}
\label{table:mc-mutants-mutators}
\end{table}

\section{MC Mutants}
\label{sec:mc-mutants}

\sys is a methodology for evaluating MCS testing environments. This approach uses cyclic candidate executions from litmus tests, as described in \mysec\ref{sec:bg-litmus}. The methodology mutates a cyclic execution by disrupting one of its edges, then generates a mutated test program by reconstructing the instructions that led to the execution. The mutated test has an execution that is allowed by the MCS but closely related to a behavior disallowed by the MCS.

To ensure that mutant programs can be reconstructed from mutant executions, the mutation process focuses on disrupting relations that correspond to the \textit{syntax} of the program, rather than relations that only arise dynamically. For example, the \textit{po} relation arises from the order of instructions in the test. The \textit{sw} relation arises from both fence instructions in the program syntax and from dynamic \textit{rf} relations caused surrounding atomic operations, but without the fences no \textit{sw} relation is possible. On the other hand, a \textit{rf} relation arises dynamically and could only be disrupted by controlling the scheduling of the threads, which is not possible in a black-box scenario. The mutations therefore encode modifications that can be implemented syntactically. Behaviors allowed by a mutated test may appear in the original test if the MCS implementation contains a bug, since the MCS requires correct executions to preserve these relations.

MC Mutants uses a set of \textit{mutator} functions and a function that converts executions into programs. A mutator consists of an abstract happens-before cycle $T$ (similar to those from prior work~\cite{alglave2010fences}) and an edge-disruption process $E$. The mutator returns $L$, a set of \textit{conformance} litmus tests containing disallowed candidate executions based on $T$, and $M$, a set of mutated tests containing closely related candidate executions for which the behavior is allowed. A testing environment can then be evaluated by running $M$: its ability to kill (i.e.\ observe) mutants in $M$ corresponds to its ability to expose an implementation error corresponding to $E$ when running $L$.


This chapter describes three distinct mutators, summarized in \myfig\ref{fig:mc-mutants-mutators}. Together, the mutators cover the syntactic edge disruptions considered in this work, i.e.\ $po$, $sw$, and $\textit{po-loc}$, and produce a complete set of mutants for the abstract happens-before cycle templates. Overall, the mutators generate 20 conformance tests and 32 mutants; the totals broken down by mutator are shown in \mytab\ref{table:mc-mutants-mutators}.

\begin{figure}
\centering
\includegraphics[width=0.6\columnwidth]{images/CORR.pdf}
\caption[CoRR candidate execution.]{A disallowed candidate execution of the Coherence of Read-Read (CoRR) litmus test.}
\label{fig:mc-mutants-corr-execution}
\end{figure}

\subsection{Mutator 1: Reversing \textit{po-loc} on Three Events \label{sec:mc-mutants-mutator-1}}

Mutator 1 takes a three-event cycle as input (LHS of \myfig\ref{fig:mc-mutants-mutator-1}). The template has two threads: thread 0 has two memory accesses ordered by $\textit{po-loc}$ (events $a$ and $b$), and thread 1 executes one memory access (event $c$). Executions of litmus tests that correspond to this cycle are disallowed by any MCS that provides SC-per-location, e.g.\ WebGPU.

The Coherence of Read-Read (CoRR) test in \myfig\ref{fig:mc-mutants-corr-execution} is one concrete instantiation of this template, where events $a$ and $b$ are reads and event $c$ is a write. The test checks whether the first read in thread 0 (event $a$) can observe the updated value written by event $c$, while the following read (event $b$) observes the stale value from the initial state. This candidate execution violates SC-per-location and is therefore disallowed by the WebGPU MCS.

The template can be instantiated by concretizing each abstract memory event (i.e.\ m[x]) as a read, write, or RMW. This also allows $com$ to be refined into one of its constituent relations (i.e.\ $rf$, $fr$, or $co$).

\paragraph{The Conformance Set} For an MCS that provides SC-per-location, a set of conformance tests can be generated by instantiating the execution for all combinations of reads, writes, and RMWs such that the cycle can be created. As a $com$ relation must contain at least one write, event $c$ must be a write or an RMW event. Then, $a$ and $b$ can be any combination of reads, writes, or RMWs.

When instantiating a test with an RMW event, only the portion of the RMW (the read or the write) that does not change the $hb$ relations of the original template is considered. For example, the second read in thread 0 of the CoRR litmus test can be replaced with an RMW, but the first cannot. An RMW is conceptually a read followed by a write, so the second read can have a trailing write while maintaining the $hb$ cycle. Replacing the first read with an RMW would conceptually insert a write between the first and second reads, which could interfere with the cycle.
%
While there may be multiple RMW variants with the same memory structure as one of the original four tests, the evaluation includes only the variant with the maximum number of plain loads and stores replaced with RMWs.

Finally, each write stores a unique increasing value. An observer thread is included for the special case in which all memory events are concretized as writes, since observing the behavior requires identifying a specific chain of $co$.

\paragraph{The Edge Disruptor} Mutator 1 disrupts the $\textit{po-loc}$ edge between $a$ and $b$ by swapping them in program order (see the right-hand side of \myfig\ref{fig:mc-mutants-mutator-1}). This removes the disallowed cycle from the original template, i.e.\ the behavior is allowed on a system that provides only SC-per-location. In fact, the behavior is allowed under sequential consistency given the execution order of $b$, $c$, $a$.

Killing these mutants requires the fine-grained interleaving of an event from thread 1 between two events of thread 0. Although this behavior may seem straightforward, it is not always easily observed, especially on GPUs. Pilot experiments show that without added stress, this fine-grained interleaving is observable on only one of the four GPUs evaluated in this study.

This mutant captures cases in which the underlying system, for example due to reorder buffers, can reorder events $a$ and $b$. If a testing environment cannot expose such fine-grained interleavings, it likely will not be effective at exposing MCS bugs. In fact, this mutant has a death rate nearly identical to the CoRR bug observed on the Apple system with an Intel GPU, discussed in \mysec\ref{sec:mc-mutants-impact}, showing that the ability to kill this mutant corresponds closely to observing a real MCS bug.

\paragraph{The Mutants} Similar to the conformance tests, the mutant tests can be obtained by instantiating the disrupted template (right-hand side of \myfig\ref{fig:mc-mutants-mutator-1}) with all possible combinations of memory accesses (reads, writes, or RMWs).

\subsection{Mutator 2: Weakening \textit{po-loc} on Four Events \label{sec:mc-mutants-mutator-2}}
Mutator 2 (\myfig\ref{fig:mc-mutants-mutator-2}) has a two-thread four-event cycle. Both threads have memory accesses ordered by $\textit{po-loc}$ and cross-thread $com$ edges.
An MCS that provides SC-per-location disallows these executions.

\paragraph{The Conformance Set} Following the rule that the $com$ relation must contain at least one write, the mutator function instantiates a set of six tests. As with Mutator 1, each write stores a unique increasing value, and an observer thread is included for the special case in which all memory events are concretized as writes.

\paragraph{The Edge Disruptor} This disruption weakens $\textit{po-loc}$ to $po$ by having $b$ and $c$ operate on a second location, $y$, instead of $x$. This turns the test into one of the weak memory tests in~\cite{alglave2010fences}. Killing the mutant then means observing a weak behavior that is allowed under a relaxed MCS, but has been shown to require stress to observe~\cite{kirkham2020foundations}.

This disruptor captures errors that might occur when memory locations are aliased or computed dynamically, as an implementation may not correctly determine that the memory locations are the same. Indeed, as \mysec\ref{sec:mc-mutants-corr-analysis} shows, one of these tests corresponds to a coherence issue previously observed on NVIDIA GPUs.

\paragraph{The Mutants} By instantiating the disrupted template in the same way as the initial template, the mutator function instantiates six mutants.

\subsection{Mutator 3: Weakening \textit{sw} on Four Events \label{sec:mc-mutants-mutator-3}}
The final mutator also uses a two-thread four-event cycle, but adds release/acquire fences (shown in \myfig\ref{fig:mc-mutants-mutator-3}).
This behavior is disallowed by an MCS that provides release/acquire synchronization and SC-per-location.

\paragraph{The Conformance Set} Using plain atomic loads and stores, the mutator instantiates three tests. These correspond to the MP with fences litmus test execution from \myfig\ref{fig:bg-mp-fenced-execution}, as well as two other common litmus test executions called load buffering and store in prior work~\cite{alglave2014herding}. Since release/acquire synchronization requires a store following a fence to synchronize with a read preceding a fence, events $c$ and $d$ must be a write and a read, respectively. This precludes the direct addition of other common weak memory litmus tests such as store buffering. However, as in Mutator 1, more tests can be instantiated using RMWs.

Replacing $c$ and/or $d$ with an RMW achieves the required synchronization. This strategy generates three additional tests, corresponding to the store buffer, read, and 2+2 write weak memory litmus test executions~\cite{alglave2014herding}. In an MCS that provides sequentially consistent fences, RMWs would not be needed to generate these three tests. Nevertheless, using release/acquire fences and RMWs to ``mimic'' sequentially consistent behavior is an important feature of an MCS to understand and test.

\paragraph{The Edge Disruptor} To disrupt this template $sw$ is weakened, removing the necessary synchronization for disallowing the weak behavior. To weaken $sw$, either one or both of the fences is removed. Removing fences is analogous to the MP with fences bug described in \mysec\ref{sec:mc-mutants-corr-analysis}, in which atomic operations were mistakenly weakened in an intermediate representation. Killing the mutant then depends on a stressful environment that can reveal weak memory behaviors, even in the presence of partial synchronization.

\paragraph{The Mutants.}
Unlike the previous two mutators, each combination of memory accesses generated using the template leads to three instantiated mutants, since either one or both fences can be removed.

\subsection{Observing Mutant Behavior \label{sec:mc-mutants-observed-mutants}} As briefly mentioned in \mysec\ref{sec:mc-mutants-introduction}, the mutation score is useful as an evaluation metric only if the mutant behavior is observable on the devices being evaluated. In some cases, the specification is more permissive than the implementation, so an implementation may receive a low mutation score even when it is being thoroughly tested. For example, very few relaxed behaviors are observable on an x86 device, while a language that targets x86 (e.g.\ C++) might allow many relaxed behaviors. Applying \sys to test C++ conformance on x86 as-is would therefore likely produce a low mutation score.

The four GPUs evaluated in this study can observe most mutant behaviors (83.6\%), so the mutation tests are retained as-is. If this were not the case, as in the C++ and x86 example, then the mutation tests should be pruned. Each mutant test $m$ should be analyzed under a \textit{precise} model of the implementation's expected observable behavior. For example, the appropriate model for x86 would be TSO~\cite{owens2009x86}. If $m$ is not observable on the implementation, then it can be pruned from the mutant test cases.

\section{Testing Environments}
Litmus tests are run repeatedly to account for the non-determinism of concurrency. They are often executed in a larger context, called a \textit{test environment} that encompasses various parameters, e.g.\  memory locations and thread affinity, which can influence the rate that different test behaviors are observed. Using \sys, testing environments can be evaluated using a mutation score and mutant death rates of the mutants generated in \mysec\ref{sec:mc-mutants}.

This section describes two improvements to state-of-the-art MCS test environments: (1)~a Parallel Test Environment (PTE), which uses the thousands of threads available in a GPU to run many test instances in parallel, and (2)~MCS Test Confidence, which provides statistical confidence around the ability of a test environment to kill a mutant given a confidence threshold or time budget.

\begin{figure}
\centering
\includegraphics[width=.9\columnwidth]{images/parallel_strategy.pdf}
\caption{A visualization of how multiple threads coordinate to run parallel instances of litmus tests.}
\label{fig:mc-mutants-parallel-ti}
\end{figure}

\subsection{Parallel Testing}
\label{sec:mc-mutants-parallel-tests}
Prior MCS testing typically ran at most a few test instances of a test at a time, since techniques were initially developed for small core-count CPUs. MCS testing on highly parallel devices, e.g.\ GPUs, inherited this single-instance design~\cite{kirkham2020foundations,alglave2015gpu} due to the difficulties in efficiently and safely scaling the number of parallel tests, especially under tunable stress testing parameters. Additionally, because GPUs sequentialize divergent control flow execution within a warp, efficient parallel tests should limit the amount of control flow divergence in the testing program. Alas, single-instance design poorly leverages the large number of threads enabled by highly parallel devices.

To address this, PTE designates a number of workgroups as \textit{testing workgroups}, in which constituent threads coordinate to run many instances of a litmus test in parallel. For example, two testing workgroups, running with 256 threads each, can run 512 instances of a two-threaded litmus test per iteration. As described in \mysec\ref{sec:mc-mutants-mut-scores}, this leads to orders of magnitude improvements in testing efficiency.

\paragraph{Implementation} The number of test instances per iteration is calculated by multiplying the number of testing workgroups by the number of threads per workgroup. Each test instance $t_i$ is then assigned memory locations to operate on. In a weak memory test like MP, $t_i$ will be assigned locations $x_i$ and $y_i$, while in a coherence test like CoRR, $t_i$ will be assigned only $x_i$. Next, two test instances $t_i$ and $t_j$ are assigned, in that order, to a thread $A$. $A$ runs thread 0's instructions from $t_i$ and thread 1's instructions from $t_j$. The other half of $t_i$ and $t_j$ are assigned to some other thread (not necessarily the same one) in the opposite order of their assignment on thread $A$, guaranteeing that all threads' instructions are executed for both $t_i$ and $t_j$.

\myfiglong\ref{fig:mc-mutants-parallel-ti} shows a configuration of test instances and memory locations assigned to threads for the MP litmus test, with fences elided. Note that no pair of test instances are assigned to the same two threads, increasing the different thread interactions of a test run. Memory locations are randomly distributed throughout memory.

\paragraph{Test Instance Assignment} There are two difficulties in implementing parallel tests on GPUs: (1) maintaining simple (i.e., non-diverged) control flow to avoid sequentialized execution and (2) pairing threads for each test instance.  PTE uses a parallel permutation function that requires only a few operations per thread, has no divergent control flow, and avoids simple patterns, such as mapping a value $n$ to $n + 1$, which has been shown to be ineffective in prior work~\cite{kirkham2020foundations}. Specifically, the function works as follows: for each value, $v$, in a sequence of $N$ consecutive natural numbers, the function produces the result $(vP)\mod{N}$, where $P$ is a number that is coprime to $N$.

As an example, thread $A$ in \myfig\ref{fig:mc-mutants-parallel-ti} might be index $i$ in the total list of threads and would execute the first set of instructions for $t_i$, storing 1 to $x_i$ and $y_i$. After executing the permutation function, $A$ would then execute the second set of instructions for $t_j$, loading $x_j$ and $y_j$. As some other thread had its native thread ID associated with the store instructions of $t_j$, each test instance is fully executed.

For a test instance $t_i$, the permutation function can also be used to spread memory locations $x_i$ and $y_i$ across the testing region by having $x$'s location associated directly with a test instance $t_i$ and permuting $y$'s location. Prior work has shown that using random native thread IDs for testing can increase weak observations~\cite{alglave2015gpu,sorensen2016exposing}. The permutation functions achieve a similar goal, stressing cache protocols that must handle thousands of threads accessing cache lines which must be kept coherent.

Lastly, if there are only two testing workgroups, at least one of $A$, $B$, or $C$ will be in different workgroups, and if there are three or more workgroups, $A$, $B$, and $C$ will all be in different workgroups. Architectures and scheduling algorithms differ across GPU vendors, so it may be advantageous to have workgroups communicate in ways that vary spatially and temporally. Therefore, test instances are \textit{striped} across workgroups, so if thread 0 in workgroup $A$ communicates with some thread in workgroup $B$, thread 1 in workgroup $B$ communicates with some thread in workgroup $C$.

\paragraph{Additional Parameters} Prior work~\cite{alglave2015gpu,kirkham2020foundations} has proposed additional heuristics and parameters for testing environments, such as shuffling thread affinities and utilizing extra threads to stress memory. These heuristics are incorporated into PTE, so that the parallel tests can also be subject to these testing techniques. Altogether, prior work~\cite{kirkham2020foundations} has provided 17 parameters that can be changed to create different testing environments. It is infeasible to examine the full space of combinations of these parameters, so a number of random configurations are run in order to find effective test environments, with \sys used to evaluate their effectiveness.

{\linespread{1.2}\selectfont
\begin{algorithm}
\small
\caption{Merging Test Environments}\label{alg:mc-mutants-cap}
\begin{algorithmic}[1]
\State{// $t$: the mutant test to find a final environment for}
\State{// $E$: the set of environments that the mutant ran in}
\State{// $D$: the set of devices that ran the mutant (across the environments $E$)}
\State{// $r$: the reproducibility score target, $0 < r < 1$}
\State{// $b$: the maximum time the test should run, $ b > 0$}
\Function{MergeEnvironments}{$t$, $E$, $D$, $r$, $b$}
\State{$ceilingRate \gets \frac{\lceil -\log_{e}(1 - r) \rceil}{b}$}
\State{$e_r \gets $\O}
\State{$n_r \gets -1$}
\State{$minRate_r \gets \infty$}
\For{$e \in E$}
\State{$n_c \gets 0$}
\State{$minRate_c \gets \infty$}
\For{$d \in D$}
\State{// rate() calculates the death rate of $t$ on $d$ in $e$}
\State{$rate_c \gets \text{rate}(t, d, e)$}
\If{$rate_c \geq ceilingRate$}
\State{$n_c \gets n_c + 1$}
\EndIf
\If{$rate_c > 0$}
\State{$minRate_c \gets \min(minRate_c, rate_c)$}
\EndIf
\EndFor
\If{$minRate_c \neq \infty$}
\If{$n_c > n_r \lor (n_c == n_r \land minRate_c > minRate_r)$}
\State{$e_r \gets e$}
\State{$n_r \gets n_c$}
\State{$minRate_r \gets minRate_c$}
\EndIf
\EndIf
\EndFor
\State{\textbf{return} $e_r$}
\EndFunction
\end{algorithmic}
\label{alg:mc-mutants-ceiling-rate}
\end{algorithm}
}

\subsection{MCS Test Confidence}
\label{sec:mc-mutants-rep-heuristic}
The second contribution to MCS testing environments, built on \sys, is MCS Test Confidence. Given a set of tests from \sys (both the conformance and mutated tests), MCS Test Confidence explores the trade-off space between testing time and the probability that the conformance tests will reveal a bug captured by \sys.

Prior work~\cite{kirkham2020foundations} derived an equation that relates the number of observations to the probability that the observation will be observed in subsequent runs. If a litmus test is executed $N$ times, and a behavior of interest is observed $x$ times, then the probability that a subsequent run of $N$ iterations will reveal at least one behavior of interest is given by $1 - e^{-x}$. For example, if $x$ is $3$, then there is a 95\% probability that another run of $N$ iterations will reveal that behavior of interest; that probability is called the \textit{reproducibility score}.

MCS Test Confidence builds on that insight and combines it with \sys. For example, if a testing environment can provide an average mutant death rate of 1 per second for each mutant, then the 20 conformance tests of \mysec\ref{sec:mc-mutants} require 3 seconds of testing time each (for a total of 1 minute of testing time) in order to have a reproducibility score of 95\%, i.e.\ a 95\% probability that the mutants will be killed in a subsequent test run.

This analysis can be applied to evaluate a testing environment, especially when deciding how long to run each test for. The results can then be applied when curating MCS tests for inclusion in a \textit{conformance test suite} (or CTS). A time budget for testing can be selected such that it provides sufficient confidence across many devices.

\paragraph{Per-test Specialized Testing Environments} Ideally, a test environment can be hyper-tuned
per test and per device. However, when curating tests for a CTS, it is only feasible to create a testing environment per test, as these are known at contribution time; there may be no a priori knowledge of the devices the CTS will execute on. Therefore, a test environment for a specific test needs to be effective on multiple devices. The following describes how the MCS Test Confidence approach can be used to create such a per-test specialized testing environment.

For each mutant from \sys, results are collected by running it in a set of identical test environments, generated by randomly instantiating parameters, on a variety of devices. Then, one environment per test is chosen using the function shown in \myalg\ref{alg:mc-mutants-ceiling-rate}. The idea is to choose the test environment that maximizes the number of devices on which the mutant death rate meets or exceeds some ceiling rate, which can be calculated using the desired reproducibility score and time budget targets of the MCS Test Confidence equation. The equation in line 7 is the inverse of $1 - e^{-x}$ and calculates the number of behaviors that need to be observed divided by the time budget to get a final rate.

If two environments end up killing the mutant at a high enough rate on the same number of devices, the environment that maximizes the minimum non-zero rate is chosen. This tie-breaking rule also applies when no environment reaches the ceiling rate on any device, selecting the environment with the greatest minimum non-zero rate. For mutants that reach the ceiling rate on all devices, this increases the minimum reproducibility score beyond the target on that mutant. On mutants that did not reach the target rate on at least one device, the chance that the mutant will be reproduced on that device despite missing the target reproducibility score is maximized. If the mutant is never killed in any environment on any device, the algorithm returns \O, indicating that no suitable environment was found.

Another useful property of breaking ties using the minimum non-zero rate is that it leads to \textit{stable} environments. If an environment chosen by the algorithm for a given reproducibility score $r$ and time budget $t$ meets or exceeds the target mutant death rate on all devices, the same environment will be chosen by another run of the algorithm that uses values $r', t'$ such that $r' \leq r$ and $t' \geq t$.

One other consideration when calculating reproducibility scores is the \textit{total reproducibility score}. Consider a CTS with 20 MCS tests and associated test environments, each of which has been found to kill its associated mutants with 95\% reproducibility. The chance that on any specific run all 20 mutants are killed is then $.95^{20}$, or 35.8\%.  The CTS would need to be run three times, on average, to observe a run where all 20 mutants are killed. Increasing the per-test reproducibility allows for a much more stable CTS; a per-test reproducibility score of 99.999\% and 20 tests leads to a total reproducibility score of 99.98\%, meaning that a test run will fail to kill all mutants on only 1 out of 500 runs.

\section{Evaluation}
\label{sec:mc-mutants-evaluation}
This section evaluates the testing environment innovations using \sys and compares them to prior work.

\begin{table}
\centering
\caption{The devices included in the study, along with how many compute units (CUs) they contain and a short name used throughout the text.}
\begin{tabular}{l l r l l l}
 \toprule
 \textbf{Vendor} & \textbf{Chip} & \textbf{CUs} & \textbf{Type}  & \textbf{Short}\\
 & & & &\textbf{Name} \\
 \midrule
 NVIDIA & GeForce RTX 2080 & 64 & Discrete  & NVIDIA \\
 AMD & Radeon Pro 5500M & 24 & Discrete  & AMD\\
 Intel & Iris Plus Graphics & 48 & Integrated  & Intel\\
 Apple & M1 & 128 & Integrated  & M1\\
 \bottomrule
\end{tabular}
\label{table:mc-mutants-gpus}
\end{table}

\subsection{Experimental Setup \label{sec:mc-mutants-exp-setup}}
As described in \mysec\ref{sec:mc-mutants}, 20 conformance tests and 32 associated mutants are instantiated. Test environments are evaluated on the 32 mutants, and their efficacy is measured using the mutation score and mutant death rate.
As mentioned throughout, the case-study specification is WebGPU. Empirical testing found that Google Chrome had the most stable WebGPU implementation, and thus, it was used exclusively.
%
The evaluation uses four GPU devices, summarized in \mytab\ref{table:mc-mutants-gpus}. These devices span four vendors and sample both integrated and discrete GPUs to provide a broad foundation upon which to evaluate \sys and parallel testing environments. At the time of evaluation, WebGPU was not yet supported on mobile devices.

Testing environments are tuned by randomly generating parameters and executing the mutants in each environment. Tuning is completed for Parallel Test Environments (\textbf{PTE}) and the single-instance techniques proposed in prior work~\cite{kirkham2020foundations} (\textbf{SITE}). \textbf{SITE} is strictly an improvement on the testing strategies used in \texttt{litmus}~\cite{alglave2011litmus} since it includes all of the \texttt{litmus} testing parameters (documented in~\cite[Sec.~3]{alglave2011litmus}), with the exception of parallel testing, which had not been efficiently implemented on GPUs before this work. SITE additionally includes memory-stressing threads, which \texttt{litmus} does not.

A tuning run is performed over 150 testing environments, running the SITEs for 300 iterations (executing 300 test instances) and the PTEs for 100 iterations (executing 125K test instances on average). More instances are run on SITE tests to provide them with more opportunities to kill mutants. The number of testing environments was chosen based on the ability to do tuning runs in a reasonable amount of time, e.g., overnight.

In addition to single-instance and parallel testing environments, two testing environments that perform no stress heuristics are also evaluated. The first, \textbf{SITE Baseline}, executes a single instance of the mutants for 300 iterations without any added stress and uses 32 workgroups, of which two workgroups have one thread execute each thread in the mutant. The second, \textbf{PTE Baseline}, runs parallel instances of the mutants for 100 iterations without any added stress and uses 1024 testing workgroups, each of which includes 256 threads.

The total tuning time, across all four devices, was 9.27 hours for the parallel environments and 16.38 hours for the single-instance environments.  In contrast, prior work ran tuning for multiple days on each chip~\cite{kirkham2020foundations}.  The focus on real-world impact in the form of adoption into conformance test suites encourages exploration of test environments that quickly and reliably kill mutants.

\begin{figure*}
\centering
\subfloat[Reversing \textit{po-loc} mutation scores (8 mutants)]{{\includegraphics[width=0.5\columnwidth]{images/figure5a.pdf}\label{fig:mc-mutants-5a}}}
\subfloat[Reversing \textit{po-loc} average mutant death rates]{{\includegraphics[width=0.5\columnwidth]{images/figure5b.pdf}\label{fig:mc-mutants-5b}}} \\
\subfloat[Weakening \textit{po-loc} mutation scores (6 mutants)]{{\includegraphics[width=.5\columnwidth]{images/figure5c.pdf}\label{fig:mc-mutants-5c}}}
\subfloat[Weakening \textit{po-loc} average mutant death rates]{{\includegraphics[width=0.5\columnwidth]{images/figure5d.pdf}\label{fig:mc-mutants-5d}}} \\
\subfloat[Weakening \textit{sw} mutation scores (18 mutants)]{{\includegraphics[width=0.5\columnwidth]{images/figure5e.pdf}\label{fig:mc-mutants-5e}}}
\subfloat[Weakening \textit{sw} average mutant death rates]{{\includegraphics[width=0.5\columnwidth]{images/figure5f.pdf}\label{fig:mc-mutants-5f}}} \\
\subfloat[All tests per device mutation scores (32 mutants)]{{\includegraphics[width=0.5\columnwidth]{images/figure5g.pdf}\label{fig:mc-mutants-5g}}}
\subfloat[All tests per device average mutant death rates]{{\includegraphics[width=0.5\columnwidth]{images/figure5h.pdf}\label{fig:mc-mutants-5h}}} \\
\subfloat[All devices/tests mutation scores (128 mutants)]{{\includegraphics[width=0.5\columnwidth]{images/figure5i.pdf}\label{fig:mc-mutants-5i}}}
\subfloat[All devices/tests average mutant death rates]{{\includegraphics[width=0.5\columnwidth]{images/figure5j.pdf}\label{fig:mc-mutants-5j}}}
\caption{Mutation scores and mutant death rates. \label{fig:mc-mutants-total-scores}}
\end{figure*}

\subsection{Mutation Score and Mutant Death Rates \label{sec:mc-mutants-mut-scores}}
\myfiglong\ref{fig:mc-mutants-total-scores} outlines the mutation score, defined as the total number of mutants killed in at least one test environment, and the average mutant death rate, defined as the average of the maximum death rates of each mutant in the specified category. Specifically, figures~\ref{fig:mc-mutants-5a}, \ref{fig:mc-mutants-5c} and \ref{fig:mc-mutants-5e} outline the mutation scores for each mutator, while figures~\ref{fig:mc-mutants-5b}, \ref{fig:mc-mutants-5d}, and \ref{fig:mc-mutants-5f} outline the average mutant death rate for each mutator.  Figures~\ref{fig:mc-mutants-5g} and \ref{fig:mc-mutants-5h} show the mutation score and rate averaged across all mutators, while figures~\ref{fig:mc-mutants-5i} and \ref{fig:mc-mutants-5j} show the mutation scores and mutant death rates averaged across all mutation types and devices.

\subsubsection{Results Summary}
This subsection first summarizes the performance of PTE compared to SITE in terms of mutation score and mutant death rate.  The results show that PTE outperforms SITE by both mutation score and by mutant death rate.  Across all devices and all tests, PTE achieves a mutation score of 83.6\%, whereas SITE achieves a score of 46.1\%, an improvement of 81.4\% (see \myfig\ref{fig:mc-mutants-5i}). \myfiglong\ref{fig:mc-mutants-5i} also outlines the importance of stress; SITE-baseline only achieves a mutation score of 6.3\%, while PTE-baseline only achieves a mutation score of 72.7\%.

PTE is even more impressive when considering mutant death rate, shown in \myfig\ref{fig:mc-mutants-5j}.  PTE achieves an average mutant death rate across all tests of 35K mutant behavior observations per second, $2731\times$ better than the mutant death rate of SITE.

\subsubsection{Detailed Analysis}
Next, a detailed analysis is provided over the results shown in \myfig\ref{fig:mc-mutants-total-scores}.

\paragraph{Mutation Score} PTE achieves a higher mutation score on 3/4 architectures (AMD, NVIDIA, and M1), which enables PTE to provide a higher aggregate mutation score than SITE. In particular, SITE does very poorly on NVIDIA and M1, where it is unable to kill any weakening \textit{po-loc} mutants (see \myfig\ref{fig:mc-mutants-5c}), and only rarely kills weakening $sw$ mutants (see \myfig\ref{fig:mc-mutants-5e}). SITE outperforms PTE on Intel in the experiments, but this is not a fundamental limitation of PTE since SITE is a strict subset of PTE. There exists a PTE equivalent to each SITE, as PTE allows environments that execute only one instance of the test per iteration. Rather, the testing did not identify a PTE that matches the outperforming SITE because the state space of PTE is much larger.

\paragraph{Mutant Death Rate} PTE achieves a higher mutant death rate on all architectures. SITE does poorly on NVIDIA and M1 (see \myfig\ref{fig:mc-mutants-5h}), largely because of a rate of 0 mutants per second for $sw$ and weakening \textit{po-loc} mutants.

\paragraph{Stress and PTE} The results show that stress improves the performance of PTE in the aggregate, improving the mutation score from 72.7\% for PTE-baseline to 83.5\% for PTE and improving the average mutant death rate from 24,400 for PTE-baseline to 35,000 for PTE.  However, these improvements are not uniform across devices; stress offers some benefit to PTE mutation scores and mutant death rate on Intel and AMD, no benefit to PTE in terms of mutation scores on NVIDIA, and only negligibly benefits PTE in terms of mutant death rate on NVIDIA.  Finally, stress improves the mutation score of PTE on M1, but decreases the mutant death rate.

\paragraph{Effectiveness Across Mutators} The highest mutant death rates occur on reversing \textit{po-loc} (\myfig\ref{fig:mc-mutants-5b}) mutants, with average rates of 22K, 58K, 428K, and 6.5K mutants killed per second on Intel, AMD, NVIDIA, and M1 respectively. The lowest rates occur on the weakening \textit{sw} (\myfig\ref{fig:mc-mutants-5h}) mutants. This is to be expected as the reversing \textit{po-loc} mutator produces mutants that are allowed under sequential consistency and requires only fine-grained interleavings to kill the mutants. On the other hand, killing a mutant from the weakening \textit{sw} mutator requires observing a weak behavior, potentially with partial synchronization, i.e.\ a fence on one of the threads.

\begin{figure}
\centering
\includegraphics[width=.9\columnwidth]{images/merged.pdf}
\caption{Analyzing the impact of time budgets and reproducibility targets on mutation scores.}
\label{fig:mc-mutants-budgets}
\end{figure}

\subsection{Reproducible Mutants \label{sec:mc-mutants-rep-mutants}}
This subsection next evaluates whether MCS test confidence enables the creation of a single test environment for each litmus test that is suitable for testing multiple devices, e.g.\ as is required by a CTS (see \mysec\ref{sec:mc-mutants-rep-heuristic}).

\myfig\ref{fig:mc-mutants-budgets} shows the results of combining test environments per test using different time budgets and two reproducibility score targets, 95\% and 99.999\%. 95\% reproducibility corresponds to killing a mutant 3 times in the allotted time budget and leads to a total reproducibility of 36.5\% with 20 tests, as explained in \mysec\ref{sec:mc-mutants-rep-heuristic}. Due to this relatively low reproducibility, 95\% is considered a lower bound on an appropriate reproducibility score target. 99.999\% is chosen as the maximum target; it corresponds to running a specific litmus test once per minute for a year and only seeing 5 non-reproducible runs, and leads to a total reproducibility of 99.98\% with 20 tests.

PTE achieves an 82\% mutation score with a 64 second time budget and a reproducibility target of 99.999\%. In contrast, SITE only achieves a mutation score of 43\% with the same constraints. PTE testing environments are almost twice as effective at killing mutants when using a single test environment across architectures.

The difference between PTE and SITE is even more striking at lower testing time budgets.  SITE mutation scores degrade rapidly as the time budget decreases, until the rate drops to zero at $1/32$ of a second. In contrast, PTE is still able to kill 36\% of mutants with 95\% reproducibility when given a time budget of $1/1024$ of a second.  Moreover, PTE achieves roughly the same mutation score as SITE's maximum score (43\%) when given a time budget of only $1/64$ of a second--i.e. PTE achieves the same mutation score with $(1/4096)^{\text{th}}$ the time budget. This is an important result for large software CTS consideration, as testing time is at a premium.

The lowest rate at which any mutant is killed in any parallel environment is one in 4.82 seconds. This means that using a time budget of 64 seconds and a reproducibility target of 99.999\%, the rightmost value on the x-axis of \myfig\ref{fig:mc-mutants-budgets} maximizes the mutants that can be killed with test environments combined per test. The mutation score at this level of reproducibility is 82\%. The single-instance mutation score for the same level is only 43\%, showing that combined parallel testing environments are almost twice as effective at killing mutants at this rate.
%%

The combined mutation score can also be compared to the mutation scores for all tests from \myfig\ref{fig:mc-mutants-total-scores}. For parallel test environments, recall that this mutation score was 83.5\%. What this means is that there are two mutants which are only killed on one device using a test environment that does not kill mutants on any other devices. For example, imagine that one test environment kills a mutant on device $A$, but not on devices $B$ and $C$, while the reverse occurs on another test environment. The combination strategy will end up choosing the test environment that kills two mutants, sacrificing (or rather saving) the mutant that was killed on device $A$. The two mutants from \myfig\ref{fig:mc-mutants-total-scores} that cannot be killed in \myfig\ref{fig:mc-mutants-budgets} are weakening \textit{sw} mutants that include a barrier and correspond to the 2+2 write and store litmus tests. These mutants are killed on AMD in at least one test environment from the tuning run, but cannot be killed when environments are combined. This again shows the importance of future work on analyzing the impact of mixed control/memory barriers on GPU behaviors and bugs.

Using the single-instance test environments, the combined mutation score in \myfig\ref{fig:mc-mutants-budgets} is 43\%, 3\% less than the maximum score of 46\% in \myfig\ref{fig:mc-mutants-total-scores}. While combining parallel test environments results in only two fewer mutants being killed, combining single-instance test environments results in four fewer mutants being killed, reinforcing the importance of parallel testing.

The merged parameter sets are the ones then recommended for inclusion in a CTS to catch MCS bugs. An issue arises when considering weakening \textit{sw} mutants, however. As the mutation function for this mutator returns a set of mutants per conformance test, it may not be initially clear which test environment should be included along with the conformance test as part of the CTS. To resolve this, environments can be chosen based on the following criteria. (1)~If it happens that the best combined test environments for each of the mutants corresponding to the same conformance test are the same, then that one test environment should be included. (2)~If, on the other hand, there are distinct test environments for different mutants, the conformance test should be run under both test environments as part of the CTS. While this adds additional running time to the CTS, this is the best strategy to catch bugs where only one of release stores or acquire loads are compiled or run incorrectly.

\begin{table}
\centering
\caption{The Pearson Correlation Coefficient (PCC) between killing mutants and observing real bugs. Values for PCCs range from -1 to 1, and while interpretations vary across domains, correlations greater than .8 are generally considered to be very strong.}
\begin{tabular}{l l l r}
 \toprule
 \textbf{Vendor} & \textbf{Failed Test} & \textbf{Mutant Type} & \textbf{PCC} \\
 \midrule
 Intel & CoRR & Reversing \textit{po-loc} & .996 \\
 AMD & MP with fences & Weakening \textit{sw} & .967 \\
 NVIDIA & MP-CO & Weakening \textit{po-loc} & .893 \\
 \bottomrule
\end{tabular}
\label{table:mc-mutants-corr-analysis}
\end{table}

\subsection{Correlation Analysis\label{sec:mc-mutants-corr-analysis}}

This work discovered and reported two bugs to the platform maintainers. One of these, observing disallowed behaviors in the MP with fences test, was only uncovered on AMD devices using PTE. This bug led to both a fix to AMD Vulkan drivers and a change to the WebGPU specification~\cite{webgpu-spec-change}. The second, observing disallowed behaviors in the CoRR litmus test on Intel GPUs running WebGPU's implementation in Chrome over Apple's Metal API, was reported to Apple.

A correlation analysis between real-world bugs and \sys was performed to determine whether killing a mutant correlates with the ability to identify real MCS bugs. The analysis uses the two new bugs and a recreated coherence violation in NVIDIA Kepler GPUs~\cite{alglave2015gpu}. This previous coherence violation manifests as a violation of one of the weakening \textit{po-loc} (\myfig\ref{fig:mc-mutants-mutator-2}) conformance tests, in which thread 0's memory accesses are writes, and thread 1's memory accesses are reads. This test is called message passing coherence (MP-CO).

The conformance test that reveals each bug and the test's associated mutant(s) were executed for 100 iterations in 150 random parallel testing environments. \mytab\ref{table:mc-mutants-corr-analysis} shows the Pearson Correlation Coefficient (PCC), which measures linear correlation and ranges from -1 to 1, between the mutant death rate and the observed bug rate across all the environments for each bug.

The results indicate high correlation between killing mutants and identifying MCS bugs.  In particular, the PCC for each bug is larger than .89; according to the Student's t-test, the probability of such a PCC occurring due to random chance is less than $10^{-6}$\%. Thus, the results show that a testing environment's ability to kill mutants (generated by \sys) correlates with its ability to find real bugs in the conformance test suite (also generated by \sys). This case study uses a mutant from each of the three mutators and spans three GPU vendors (AMD, NVIDIA, and Intel), showing that all the mutators are effective and the approach is portable across the diverse backends targeted by WebGPU.

\subsection{Impact\label{sec:mc-mutants-impact}}

This work led to the discovery and reporting of two MCS bugs to platform maintainers. Additionally, the work on improving test environments has led to the official WebGPU CTS adopting PTE for MCS testing~\cite{webgpu-cts}. The WebGPU CTS is part of Chromium's continuous integration process as development on WebGPU compilers continues, and runs on a variety of GPUs (e.g.\ NVIDIA, Intel) and platform implementations (e.g.\ Vulkan, Metal, Direct3D). The diversity of devices is expected to grow as more browsers and platforms, especially mobile ones, implement the WebGPU standard. At least one parallel test has also been included in the Vulkan CTS as a result of the bug found on AMD drivers~\cite{vulkan-parallel-tests}.
